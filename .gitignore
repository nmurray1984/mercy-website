node_modules/
# Anchored to the repo root so this ignores the *generated* public site only,
# not server/public/ (the admin's static assets, which must be committed).
/public/
logs/
data/
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
.env
.DS_Store

# Test artifacts
coverage/
playwright-report/
test-results/
tests/.playwright-tmp/
