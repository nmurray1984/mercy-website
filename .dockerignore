# Build the image from source only — never copy host artifacts.
node_modules/
# Anchored to root: keep the generated site out of the image, but still copy
# server/public/ (the admin's static assets).
/public/
data/
logs/
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
.env
.git/
.gitignore
.DS_Store

# Tests / dev-only
tests/
coverage/
playwright-report/
test-results/
playwright.config.js
vitest.config.js

# Docs / planning
PLAN.md
requirements.html
